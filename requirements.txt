# ---------------------------------------------------------------------------
# One Python 3.9 conda environment runs the whole pipeline (see environment.yml,
# which adds a C/C++ compiler so pip can build pyradiomics).
#
# Verified together on Python 3.9 (numpy 1.26, torch 2.6, monai 1.5,
# pyradiomics 3.1, tabpfn 7.1.1 / tabpfn-extensions 0.3.0).
# ---------------------------------------------------------------------------

# Core scientific stack
numpy>=1.23,<2          # pyradiomics C extensions need the numpy 1.x ABI
pandas>=1.5
scipy>=1.9
scikit-image>=0.20
SimpleITK>=2.2
PyYAML>=6.0
tqdm>=4.64

# Radiomics shape features (pip-only; built from source if no wheel)
pyradiomics>=3.0.1

# Local thickness
localthickness>=0.1.3        # GPL-3.0; used as an external dependency only
matplotlib>=3.5              # imported by localthickness at module load

# Segmentation (SwinUNETR) — match the torch build to your CUDA
torch>=2.5.0,<2.7
monai>=1.5                   # SwinUNETR has no img_size argument in >=1.5
einops>=0.6
nibabel>=5.0

# Modeling
scikit-learn>=1.1
xgboost>=1.7
lightgbm>=3.3
optuna>=3.0                  # hyper-parameter search (mode=hpo)
# TabPFN model v2.5 is provided by the 7.1.1 package
tabpfn-extensions[hpo]==0.3.0   # TabPFN HPO (needs hyperopt) + post-hoc ensembling
